import { NewUserSelectDirective } from './new-user-select.directive';

describe('NewUserSelectDirective', () => {
  it('should create an instance', () => {
    const directive = new NewUserSelectDirective(null, null);
    expect(directive).toBeTruthy();
  });
});
